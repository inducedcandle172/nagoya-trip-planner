{
  "name": "nagoya-trip-planner",
  "version": "1.0.0",
  "description": "Nagoya Trip Planner App",
  "main": "index.js",
  "scripts": {
    "start": "npx serve .",
    "build": "npm run build:web && npm run build:android",
    "build:web": "echo 'Add your web build process here'",
    "build:android": "npx cap sync && npx cap copy android",
    "open:android": "npx cap open android"
  },
  "keywords": [
    "travel",
    "itinerary",
    "nagoya",
    "trip-planner"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "serve": "^14.0.0"
  }
}
